<% content_for :title do %>
	<%= title %>
<% end %>
<div class="warp">
	<div class="report-img">
		<%= image_tag "report-img.png" %>
	</div>
	<div class="list">
		<%= link_to "接口列表", "/reports/#{@customer.id}/#{route}?date=#{date}", :class => "btn btn-mysterious btn-lg btn-block" %>
		<%= link_to "用户列表", "/customers/#{@customer.id}/api_users?route=#{route}&date=#{date}", :class => "btn btn-mysterious btn-lg btn-block" %>
	</div>
</div>
